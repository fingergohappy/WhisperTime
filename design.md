1. 功能模块

项目管理

创建/编辑项目。

每个项目有多条“计时记录”。

计时器

单任务启动，正计时或倒计时。

前台服务保证后台运行。

可设置语音播报间隔。

计时记录

每次计时结束后，保存为一条记录（开始时间、结束时间、持续时长）。

支持手动修正时间（修改持续时长或开始/结束时间）。

可以查看统计数据，比如总时间、平均时间、记录列表。

语音播报

按设定间隔播报剩余时间或已用时间。

TTS 队列管理避免重叠。